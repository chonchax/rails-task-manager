<%= link_to "Edit Task", task_edit_path  %>

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<i class="fa-solid fa-rotate-left"></i>
<%= link_to "retour aux tasks", tasks_path  %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Delete #{@task.title.capitalize}?"}
%>
