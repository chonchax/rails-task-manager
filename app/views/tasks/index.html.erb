<ul>
  <% @tasks.each do |task| %>

    <li>
      <%= link_to task.title, task_path(task.id) %>
      <%= link_to  task_path(task), data: {turbo_method: :delete, turbo_confirm: "Delete #{task.title.capitalize}?" } do %>
        <i class="fa-solid fa-trash-can"></i>
      <% end %>

    </li>
  <% end %>
</ul>

<div>
  <i class="fa-solid fa-plus"></i>
  <%= link_to "Créer un nouvelle task", tasks_new_path   %>
</div>
